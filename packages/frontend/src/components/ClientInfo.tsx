import React from 'react';

interface Props {}

// types
// default
// suggestion
// username-only
// chat-info

// is_verified_show (default = false)
// is_story_show (default = false)

export const ClientInfo = (props: Props) => {
  return <div></div>;
};
