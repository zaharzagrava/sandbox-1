import React from 'react';

interface Props {}

const EmailsSent = ({}: Props) => {
  return (
    <div style={{ textAlign: 'center' }}>
      Emails from Instagram tab element stub :)
    </div>
  );
};

export default EmailsSent;
